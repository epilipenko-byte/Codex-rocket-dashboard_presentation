# CODEX_EDITION_2_2026-07-07 — checkpoint

Статус: Edition 2 опубликована на GitHub Pages; Apps Script proxy Edition 2 развернут; фронт переключен на новый JSONP proxy; правки 2026-07-08 по периодам, cash, D/E, активам баланса и live-loading опубликованы и проверены на публичной GitHub Pages ссылке. После фиксации рабочей точки начаты доработки Edition 2.x.

**Зафиксированная рабочая версия:** Edition 2 / `edition-2-2026-07-07`, Apps Script web app version 27. Эта версия считается рабочей стабильной точкой: сайт открывается по GitHub Pages, принимает пароль `password`, получает `LIVE — Google Sheets`, показывает 13 вкладок и корректно отображает ключевые KPI июня 2026. Дальнейшие правки вести поверх нее, не перезаписывая Edition 1.

Обновление 2026-07-09 02:15 +03: локально подготовлен новый пакет правок после утренних замечаний пользователя; Apps Script web app обновлен до версии 27; proxy проверен по live URL.

Обновление 2026-07-09 14:14 +03: Apps Script web app обновлен до версии 28. Proxy теперь читает служебный источник `dds_operations`, даже если он скрыт в Dashboard Config (`visible=false`), потому что этот лист нужен для остатков денежных средств по кошелькам. Локальная проверка подтвердила `dds_operations_data`: 3000 строк, строка `Остатки всего`, общий остаток около `$41K` на `09.07.2026`.

Обновление 2026-07-09 14:30 +03: локально исправлен `Тех. лист`: `Dashboard Config / Reports`, `Threshold Rules` и `Notes` отображаются как таблицы из объектов `tech_sheet`, ссылки становятся активными. Причина прежней пустоты: фронт пытался отрисовать объектный `tech_sheet` как двумерную таблицу.

## Локально готово

- Автономный пакет Edition 2 создан.
- Edition 1 документы включены в пакет без изменений.
- Новый протокол Edition 2 создан.
- Новый requirements/playbook/resource links созданы.
- Фронт Edition 2 обновлен:
  - пользовательский UI без слова `Codex`;
  - первая вкладка переименована в `Обзор месяца`;
  - карточки `Обзор месяца` показывают последний месяц выбранного периода и сравнение с предыдущим месяцем;
  - отчетные вкладки остаются накопительными за выбранный период и показывают “в т.ч. последний месяц” в подписях карточек;
  - вкладки `Проекты Rocket`, `Ludens Group`, `Тех. лист`;
  - `Задачи` объединены с `Результаты`;
  - маржинальности в обзоре и P&L берутся как source-percent rows / fallback;
  - процентные строки источника нормализуются: если Google Sheets отдает `0.37`, фронт показывает `37%`, а не `0.37%`;
  - cash показывается как `period_end`;
  - P&L waterfall исправлен по знакам;
  - расходы сгруппированы;
  - ДДС использует `Total NCF` / fallback;
  - ДДС, баланс, трафик, проекты Rocket и Ludens Group получили раскрываемые таблицы;
  - ЦКП строится блоками: ЦКП, показатели, диаграмма, заметки докладчика;
  - `Результаты` снова показывают полный блок пороговых правил / целевых метрик;
  - `Плат.календарь` получил сигнальные карточки план/факт поступлений, выплат и cash;
  - заметки докладчика привязаны к визуальным блокам и сохраняются через Apps Script POST;
  - техлист показывает reports, matching, ссылки, ошибки и период.
- Apps Script proxy локально обновлен:
  - процентные строки;
  - `total_ncf`;
  - `dashboard_links`;
  - `speaker_notes`;
  - `tech_sheet`;
  - `write_speaker_note`.
- Apps Script proxy после утренних замечаний 2026-07-08 дополнительно исправлен:
  - D/E больше не может матчиться на строки кошельков вроде `Vivid (EUR)`;
  - D/E подтягивается из строки `Финансовый рычаг = Заемный капитал/ ср. Собственный капитал D/E`;
  - `assets_total` подтягивается из строки `Активы ↓`, а не из расчетных строк с фразой `Валюта баланса`;
  - cash подтягивается из строки `Денежные средства`;
  - `Total NCF` подтягивается из строки `NCF Изменение денег за месяц (общее)`.
- Apps Script proxy после ночных замечаний 2026-07-09 дополнительно исправлен:
  - `var_costs` больше не может матчиться на строки `Затраты на трафик` / `traffic`;
  - `var_costs` требует токен `переменн` / `variable`;
  - удален fallback, который подставлял `traffic_cost` вместо ненайденных переменных расходов;
  - live-проверка proxy: `var_costs` матчится на строку `Переменные`, `traffic_cost` отдельно на строку `Затраты на трафик`;
  - активная версия Apps Script web app: 27.
- Фронт после утренних замечаний 2026-07-08 дополнительно исправлен:
  - живой ответ очищает резервные demo-поля перед заполнением, чтобы старые значения не оставались в `lt_debt/st_debt` и других KPI;
  - период по умолчанию не расширяется до частично заполненного июля только из-за cash или Total NCF;
  - default period строится по полезным P&L-месяцам;
  - если отдельные строки долга не найдены, карточка D/E показывает долг как `D/E × капитал`, а не старый резервный долг;
  - timeout live-загрузки увеличен до 180 секунд, чтобы полный proxy-ответ не выглядел как зависшая загрузка.
- Фронт после ночных замечаний 2026-07-09 дополнительно исправлен:
  - таблицы источников больше не создают искусственную группировку для ДДС, баланса, платежного календаря и трафика; эти листы показываются одной свернутой исходной таблицей;
  - `Проекты Rocket` сохраняет проектные секции из исходного листа: `Гэмблинг`, `Сторонние команды`, `Rocket Streams`, `Rocket Influence (RTP)`, `Mannequin`, `Тесты новых источников`;
  - все source details стартуют свернутыми;
  - `Обзор месяца` не показывает `source row` в KPI;
  - карточка чистой прибыли в обзоре не дублирует подпись про маржинальность;
  - если старый proxy/кэш временно присылает `var_costs` как процентную строку, фронт защитно восстанавливает сумму переменных расходов через строку маржинальности;
  - вкладка `Трафик` читает FTD и Dep count из исходной матрицы traffic-листа по строкам `-ftd месяц` и `-dep месяц`;
  - вкладка `Трафик` заменяет пустой CRM-график на `FTD и депозиты`.
- Фронт после фиксации рабочей точки 2026-07-09 дополнительно исправлен:
  - вкладка `Плат.календарь` показывает текущий cash по кошелькам из `dds_operations`, а не общий cash из сводного ДДС/баланса;
  - добавлена таблица `Остатки денежных средств по кошелькам`;
  - локальная проверка: карточка `Текущий кэш по кошелькам` показывает `$41K`, дата `09.07.2026`, в таблице видны `USDT (резерв)`, `РС (USDT)`, `Касса (USD)`, `РС (Кипр)`, `Vivid (EUR)` и другие кошельки.
  - `Тех. лист` локально показывает `Dashboard Config / Reports` в форме таблицы с активными ссылками на исходные Google Sheets.
- Apps Script proxy JSONP deployment:
  `https://script.google.com/macros/s/AKfycbxKamFtHfX6VZZE2OcVkaPgHHygP8Ck0Pt9SrLYZeBHXDQi5Qqi-rU5dchVw2opZkAq/exec`
- Проверка proxy: HTTP `200`, возвращает `month_keys`, `total_ncf`, `tech_sheet`, `metric_diagnostics`; JSONP callback проверен через `callback=cbtest`.
- Время ответа proxy при полной живой сборке: около 60-110 секунд, поэтому фронт использует JSONP, быстро показывает кэш при наличии локального снимка и не блокирует экран.
- Проверка опубликованной страницы после JSONP: бейдж переключается с `КЭШ — Google Sheets` на `LIVE — Google Sheets`, период по умолчанию `Янв—Июн 2026`.
- Синтаксис `index.html` JavaScript и `apps_script_proxy.js` проверен.
- Локальный preview screenshot:
  `work/rocket-dashboard/edition2-local-overview.png`
- GitHub Pages upload bundle:
  `CODEX_EDITION_2_2026-07-07__GITHUB_PAGES_UPLOAD.zip`
- Распакованная папка для загрузки:
  `publish_bundle/edition-2-2026-07-07/`

## Публикация

Опубликовано в GitHub repository:

`epilipenko-byte/Codex-rocket-dashboard_presentation`

Папка публикации:

`edition-2-2026-07-07/`

GitHub Pages ссылка:

`https://epilipenko-byte.github.io/Codex-rocket-dashboard_presentation/edition-2-2026-07-07/`

Commit:

`3e5177ee124ff1bc9093c68d47158e334b7226fb`

Commit message:

`Add Edition 2 dashboard 2026-07-07`

Documentation commits:

- `854b525` — `Add Edition 2 documentation package`, uploaded Markdown handoff package to `edition-2-2026-07-07/docs/`.
- `c3faf07` — `Add Edition 2 source requirements docs`, uploaded source Word TZ documents to `edition-2-2026-07-07/docs/source/`.
- `6a3f418` — `Update Edition 2 dashboard live data matching`, uploaded updated `index.html` and `apps_script_proxy.js`.
- `e65a73f` — `Update Edition 2 documentation after live checks`, uploaded updated Edition 2 documentation package.
- `462f544` — `Update Edition 2 dashboard overnight fixes`, uploaded updated `index.html` and `apps_script_proxy.js`.
- `14b63e3` — `Update Edition 2 documentation overnight fixes`, uploaded updated Markdown documentation package.

Проверка опубликованной страницы:

- HTTP status: `200`
- title: `Rocket Finance Dashboard — Edition 2`
- парольный вход найден и проверен;
- пароль `password` принят;
- вкладки `ПРОЕКТЫ ROCKET`, `LUDENS GROUP`, `РЕЗУЛЬТАТЫ`, `ТЕХ. ЛИСТ` отображаются;
- пользовательский интерфейс не показывает слово `Codex`.
- после правок 2026-07-08 публичная страница проверена с cache-buster:
  - URL: `https://epilipenko-byte.github.io/Codex-rocket-dashboard_presentation/edition-2-2026-07-07/`;
  - пароль `password` принят;
  - статус `LIVE — Google Sheets`;
  - период по умолчанию `Янв—Июн 2026`;
  - отображаются 13 вкладок;
  - карточки обзора берут последний выбранный месяц, на момент проверки июнь 2026;
  - D/E подтягивается из строки `Финансовый рычаг = Заемный капитал/ ср. Собственный капитал D/E`;
  - cash подтягивается из строки `Денежные средства`;
  - активы баланса подтягиваются из строки `Активы ↓`.

Дата проверки сайта: `2026-07-08 00:31 +03`.

Дата проверки полного GitHub-пакета: `2026-07-08 00:36 +03`.

Дата повторной проверки после утренних исправлений: `2026-07-08 13:27 +03`.

Дата повторной проверки после ночных исправлений и публикации: `2026-07-09 02:25 +03`.

Проверка опубликованной страницы 2026-07-09:

- URL: `https://epilipenko-byte.github.io/Codex-rocket-dashboard_presentation/edition-2-2026-07-07/`;
- пароль `password` принят;
- статус `LIVE — Google Sheets`;
- обзор показывает июнь 2026: выручка `$652K`, чистая прибыль `$89K`, маржинальная рентабельность `42.2%`, операционная `15.5%`, ЧП `13.6%`, D/E `2.01`, cash `$95K`;
- `source row` не отображается в пользовательских KPI;
- ОПиУ показывает переменные расходы за период `$3.04M`, июнь `$377K`;
- `Проекты Rocket` показывает 6 свернутых проектных секций с исходными названиями;
- `Ludens Group`, ДДС, баланс, платежный календарь и трафик показывают свернутые исходные таблицы без искусственной перегруппировки;
- `Трафик` показывает FTD июнь `537`, Dep count июнь `2 971`, FTD период `7 025`;
- `ЦКП` показывает 6 блоков и свернутую полную исходную таблицу;
- `Результаты` показывает внешние ссылки и блок `Целевые метрики и пороговые правила`.

## История публикации

Ранее был подготовлен локальный git commit в:

`/tmp/rocket-edition2-publish`

Push не выполнен, потому что терминал не имеет GitHub credentials:

`fatal: could not read Username for 'https://github.com': terminal prompts disabled`

После паузы временный clone `/tmp/rocket-edition2-publish` может отсутствовать, но исходные файлы и upload bundle сохранены в пакете Edition 2.

GitHub connector также не смог записать файл в репозиторий:

`GitHub API error 403: Resource not accessible by integration`

Это означает, что доступный коннектор имеет недостаточно прав для записи в этот репозиторий.

GitHub web upload открыт в Chrome по адресу:

`https://github.com/epilipenko-byte/Codex-rocket-dashboard_presentation/upload/main/edition-2-2026-07-07`

Файлы для выбора:

- `publish_bundle/edition-2-2026-07-07/index.html`
- `publish_bundle/edition-2-2026-07-07/grass_bg.html`
- `publish_bundle/edition-2-2026-07-07/apps_script_proxy.js`

Попытка программной загрузки через Chrome file chooser остановлена ограничением расширения:

`fileChooser.setFiles failed: Not allowed`

Для автоматической загрузки файлов через Chrome нужно включить разрешение расширения Codex:

`chrome://extensions` -> Codex -> Details -> `Allow access to file URLs`.

Также macOS не разрешила агенту управлять системным окном выбора файлов без Accessibility permission для `osascript`.

После включения разрешения Chrome на локальные файлы web upload через GitHub сработал.

## Должно быть готово перед финальной передачей

- Отдельная GitHub Pages ссылка Edition 2: готово.
- Отдельная публикация с `edition-2` и датой в имени: готово.
- Полный пакет Markdown-документации в GitHub: готово.
- Исходные Word-ТЗ в GitHub `docs/source`: готово.
- Обновленный Apps Script proxy: готово; текущая развернутая версия после доработки кошельков — `28`.
- Обновленный фронт: готово локально и в publish bundle; требуется публикация обновленного `index.html` на GitHub Pages.
- Проверенный пароль `password`: готово.
- Проверенные периоды.
- Проверенные P&L, ДДС, D/E, маржинальности, заметки докладчика, техлист.
- Локальная контрольная проверка 2026-07-08 после правок:
  - обзор: `Янв—Июн 2026`, `LIVE — Google Sheets`, статус `обновлено`;
  - карточки обзора: выручка июнь около `$652K`, чистая прибыль около `$89K`, маржинальная рентабельность `42.2%`, операционная `15.5%`, ЧП `13.6%`, D/E около `2.28` на момент проверки, cash около `$95K`;
  - ДДС: `Total NCF` за период около `$89K`, июнь около `$84K`, таблица ДДС раскрываемая;
  - Трафик: есть раскрываемая таблица исходного листа;
  - Результаты: есть внешние dashboard-ссылки и блок `Целевые метрики и пороговые правила`;
  - ЦКП: построены блоки `ЦКП 1` ... `ЦКП 6`, у блоков есть заметки докладчика;
  - Техлист: показывает `Dashboard Config / Reports`, `Metric matching`, source URLs, ошибки и период.
- Локальная контрольная проверка 2026-07-09 после ночных правок:
  - обзор: `LIVE — Google Sheets`, `Янв—Июн 2026`;
  - карточки обзора: выручка июнь `$652K`, чистая прибыль июнь `$89K`, маржинальная рентабельность `42.2%`, операционная `15.5%`, ЧП `13.6%`, D/E `2.01`, cash `$95K`;
  - ОПиУ: переменные расходы за период `$3.04M`, июнь `$377K`; водопад больше не показывает маржу больше выручки;
  - `Проекты Rocket`: 6 свернутых проектных секций с исходными названиями проектов;
  - `Ludens Group`: одна свернутая исходная таблица `6 месяцев 2026 г.`;
  - ДДС: `Total NCF` за период `$71K`, июнь `$66K`, таблица ДДС свернута;
  - Баланс, платежный календарь, трафик: исходные таблицы свернуты и не перегруппированы искусственно;
  - `Трафик`: FTD июнь `537`, Dep count июнь `2 971`, FTD период `7 025`;
  - `ЦКП`: построены 6 блоков ЦКП и полная исходная таблица свернута;
  - `Результаты`: блок `Целевые метрики и пороговые правила` отображается.

- Локальная step-by-step доработка 2026-07-10:
  - исходные таблицы получили более глубокое раскрытие по реальной иерархии строк, без искусственной перегруппировки источников;
  - в таблицах подписи/текстовые статьи выровнены влево, числовые ячейки выровнены вправо и используют tabular-цифры;
  - исправлена классификация строк ДДС: `Pos CF`, `Neg CF`, `NCF`, `Cash at End` остаются верхним уровнем, а не прячутся внутрь первого блока остатков;
  - debt display в обзоре и долговая структура в балансе больше не включают операционную кредиторку; долг берется как `D/E × equity`, если D/E и капитал найдены;
  - вкладка `Трафик` получила свод по источникам: выручка, трафик, маржинальный доход, ROI, FTD, Dep, ARPU, CAC из матричного source-листа;
  - ROI в traffic-summary нормализуется из коэффициента в проценты, CAC показывается по модулю;
  - `Проекты Rocket` проверены локально: 6 проектных секций отображаются отдельными свернутыми блоками с исходными названиями;
  - `ЦКП` проверены локально: 6 блоков ЦКП и 6 блоков заметок докладчика;
  - добавлены заметки докладчика на вкладки `Модель` и `Выводы`;
  - синтаксис JavaScript в `index.html` проверен через bundled Node: `scripts ok`;
  - GitHub Pages пока не обновлялся: эта точка предназначена для локальной проверки пользователем.

- Локальная step-by-step доработка 2026-07-10, следующий шаг таблиц:
  - управленческая таблица ОПиУ получила корректные раскрываемые строки: `+` показывается только у строк с дочерними данными;
  - `Вся заработная плата` и `Все IT сервисы` в переменных расходах больше не нулевые и показываются как расходы со знаком минус;
  - первая колонка source-таблиц закрепляет найденное название статьи (`Показатель`), а не пустой столбец или служебную букву из исходного листа;
  - заголовки source-таблиц начинаются с `Показатель`, далее идут месяцы/числовые колонки;
  - денежные значения в таблицах и tooltip/осях графиков форматируются в тысячах без лишних знаков после запятой;
  - локально проверено: вкладка `ОПиУ` содержит `Управленческая группировка ОПиУ` и `Раскрываемая таблица ОПиУ из источника`; `Проекты Rocket` сохраняет 6 проектных секций; `Ludens Group` показывает source-таблицу с первой колонкой `Показатель`;
  - синтаксис JavaScript и proxy повторно проверен: `scripts ok; proxy ok`;
  - GitHub Pages пока не обновлялся: эта точка остается локальной для проверки перед публикацией.

- Локальная step-by-step доработка 2026-07-10, второй этап визуализации:
  - добавлен общий слой подписей значений на столбчатых и линейных графиках; круговые и радарные диаграммы не перегружаются value labels;
  - под ключевыми диаграммами добавлены короткие презентационные подписи: что показывает график и какой вывод виден по текущим данным;
  - карточки коэффициентов получили нормы и формулы muted-текстом: D/E, автономия, текущая ликвидность, финансовая устойчивость;
  - вкладка `ЦКП` перестроена по реальной структуре source-листа: строка `Период`, блоки `ЦКП N — название`, строки `Критерии`;

- Локальная аудиторская доработка 2026-07-11 перед следующей публикацией:
  - сверка `Обзор месяца` выполнена по живым Google Sheets, proxy и фронтовой логике: выручка, ЧП, маржинальности, cash, D/E и Total NCF совпадают с источниками в пределах округления;
  - обнаружено, что live proxy диагностически матчиг `traffic_cost` на процентную строку `Затраты на трафик`, а не на денежные строки трафика; в локальном proxy добавлена защита от принятия ratio-like рядов как денежных расходов;
  - обнаружено, что live proxy не возвращает `pnl_data` для `ОПИУ: год комбо`, поэтому фронт мог подставлять fallback `pnl_consol`/`pnl_combo` в раскрываемой таблице ОПиУ; фронт локально исправлен и больше не подменяет исходный ОПиУ другим P&L-листом;
  - raw-таблицы источников локально получили фильтрацию служебных колонок: показываются выбранные месяцы и `ИТОГО`, а горизонтальный/вертикальный анализ, сценарные блоки и прочие служебные колонки не должны попадать в основную презентационную таблицу;
  - изменения пока не опубликованы на GitHub Pages и требуют повторной локальной визуальной проверки, затем деплоя Apps Script proxy и публикации новой GitHub Pages версии отдельной папкой.
  - для ЦКП учтен декабрьский столбец перед январем: месяцы сопоставляются по порядку колонок с offset, чтобы январь-июнь показывали фактические значения;
  - ЦКП теперь показывает 6 блоков, в таблице каждого блока есть критерий, последний месяц и среднее за период; график справа строится по числовым критериям;
  - локальная проверка `http://localhost:8078/?v=edition2-stage2-complete-20260710`: 13 вкладок, 32 canvas, 29 chart captions, 6 блоков ЦКП, 5 формул и 4 нормы на карточках;
  - синтаксис JavaScript и proxy проверен: `scripts ok; proxy ok`;
  - GitHub Pages пока не обновлялся: изменения подготовлены локально и в publish bundle.

- Публикация 2026-07-11 должна выполняться отдельной папкой `edition-2-2026-07-11/`, чтобы ранее опубликованная рабочая версия `edition-2-2026-07-07/` осталась доступной как быстрый откат.

- Фактическая публикация 2026-07-11 выполнена отдельной папкой:
  `https://epilipenko-byte.github.io/Codex-rocket-dashboard_presentation/edition-2-2026-07-11/`
  Старая рабочая ссылка сохранена без перезаписи:
  `https://epilipenko-byte.github.io/Codex-rocket-dashboard_presentation/edition-2-2026-07-07/`
  GitHub commits:
  - `da3c548` — `Add Edition 2 dashboard 2026-07-11`;
  - `4ec3808` — `Add Edition 2 documentation 2026-07-11`;
  - `235b065` — `Add Edition 2 source documents 2026-07-11`.
  Проверка опубликованной страницы: HTTP `200`, пароль `password` принят, 13 вкладок отображаются, статус переключился на `LIVE — Google Sheets`, ошибок загрузки нет.

- Аудиторская доработка 2026-07-13 перед публикацией следующей безопасной папкой:
  - Apps Script proxy обновлен до version 33 в том же web app URL;
  - исправлено чтение raw-таблицы ОПиУ: `pnl_data` теперь отдается из `opiu` по gid из Dashboard Config, то есть из живой вкладки `(ENG) PnL Year`;
  - исправлено разделение платёжного календаря: `payment_data` отдается из листа `Свод`, `payment_plan_data` отдельно из `Плановый реестр`;
  - лимит `dds_operations_data` увеличен с 3000 до 12000 строк; live proxy на момент проверки отдал 9464 строки;
  - для строк с процентами добавлена защита от денежных строк (`USD`, суммы, amount), чтобы маржинальности не матчились на `Contribution margin, USD`;
  - `op_margin_pct` больше не матчится на `Рентабельность по EBITDA` из проектного листа; live proxy матчится на `(ENG) PnL Year` / `Operating margin, %`;
  - локальная проверка через `http://localhost:8078/?v=edition2-final-v33-20260713`: пароль `password` принят, 13 вкладок отображаются, статус `LIVE — Google Sheets`;
  - контрольные KPI июня из live proxy и локального фронта: выручка `$652K`, чистая прибыль `$85K`, маржинальная рентабельность `42%`, операционная `15%`, рентабельность ЧП `11%`, D/E `2.06`, cash `$95K`;
  - вкладки `ОПиУ`, `Проекты Rocket`, `Ludens Group`, `ДДС`, `Баланс`, `Плат.календарь`, `Трафик`, `ЦКП`, `Результаты`, `Тех. лист` открыты локально и не пустые;
  - техлист-текст обновлен: больше не выводит устаревшее предупреждение об отсутствии source URL, так как source URL берется из Dashboard Config и показывается в загруженных источниках;
  - синтаксис `index.html` и `apps_script_proxy.js` проверен: `ok`.

## Backlog / незакрытые задачи после фиксации рабочей Edition 2

Эти задачи не блокируют текущую рабочую версию, но являются следующими доработками Edition 2.x:

1. Частично закрыто 2026-07-09: Apps Script proxy version 28 читает `dds_operations`, фронт локально показывает отдельную карточку и таблицу остатков по кошелькам. Осталось опубликовать обновленный фронт на GitHub Pages и проверить публичную страницу.
2. Частично закрыто 2026-07-09 локально: техлист показывает `Dashboard Config / Reports`, `Threshold Rules`, `Notes`, source URLs и ошибки; ссылки активные. Осталось опубликовать обновленный фронт и проверить публичную страницу.
3. Локально частично закрыто 2026-07-10: сделать более глубокое раскрытие исходных таблиц по их реальной иерархии, где это возможно без искусственной перегруппировки. Осталось визуально принять пользователем и при необходимости точечно поправить уровни вложенности на отдельных листах.
4. Локально частично закрыто 2026-07-10: сделать debt breakdown только по процентному долгу / займам / interest-bearing debt; не включать операционную кредиторку. Осталось сверить формулу на живом балансе при следующем полном контрольном проходе.
5. Локально частично закрыто 2026-07-10: расширить вкладку `Трафик` по источникам: выручка, затраты, маржинальный доход, ROI, ARPU/CAC, FTD, Dep. LTV выводить только если source-лист явно вернул эту строку.
6. Локально проверено 2026-07-10: ЦКП строятся как 6 блоков и визуально разделены. Осталось бизнес-сверка пользователем по содержанию показателей.
7. Частично закрыто 2026-07-10: заметки докладчика есть на пользовательских вкладках и добавлены на `Модель`/`Выводы`. Осталось финально принять расположение блоков после визуального просмотра.
8. Провести ручную контрольную сверку живых KPI с исходными Google Sheets по ячейкам/строкам для зон риска: баланс, D/E/debt, cash, платежный календарь, трафик.
9. Отдельно проработать дизайн-варианты шрифтов/цветовой схемы для следующей версии, не ломая текущую Executive Dark версию.
